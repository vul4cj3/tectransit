import { ShippstatusPipe } from './shippstatus.pipe';

describe('ShippstatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ShippstatusPipe();
    expect(pipe).toBeTruthy();
  });
});
